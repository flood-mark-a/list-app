version: '2'
services:
  list-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/List/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=List
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
